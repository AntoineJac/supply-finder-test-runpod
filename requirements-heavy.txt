--extra-index-url https://download.pytorch.org/whl/cu124

# --- 1. PyTorch (CUDA 12.4) ---
torch==2.6.0+cu124

# --- 2. numpy compatible with both torch 2.6 and infinity-emb ---
numpy>=1.20.0,<2.0.0

# --- 3. huggingface-hub pinned below 1.0 ---
#   infinity-emb 0.0.77 eagerly imports HfFolder from huggingface_hub in
#   transformer/utils.py (via utils_optimum.py) at module load time.
#   HfFolder was removed in huggingface-hub==1.0.0.
#   Pin to the latest 0.x release to avoid the ImportError.
#   All constraints satisfied: infinity-emb>=0.32.0, transformers>=0.34.0
huggingface-hub==0.36.2

# --- 4. infinity-emb with torch extra ---
#   [torch] → sentence-transformers, tokenizers, transformers
infinity-emb[torch]==0.0.77